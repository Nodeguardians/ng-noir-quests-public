mod tests; // Do not modify this first line!

fn main(
    secret: Field,
    index: Field,
    merkle_proof: [[u8; 32]; 3],
    merkle_root: pub [u8; 32],
    nullifier: pub Field,
) {
    // Implement this
}
